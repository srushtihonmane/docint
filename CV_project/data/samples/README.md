# Sample images

Everything in this folder is **gitignored except** this README and files
matching `sample*.jpeg` — keep those small (< 300 KB) so the repo stays
light. Current reference set:

- `sample1.jpeg`
- `sample2.jpeg`
- `sample3.jpeg`

Good reference photos to keep here: a printed English page with mild skew
and shadow, an exam paper with `Q1 ... (a)...(d)` blocks, and a printed
Marathi/Devanagari page.

Larger evaluation sets belong in `data/eval/` (fully gitignored) — see
`eval/benchmark.py` for the `manifest.csv` format.
